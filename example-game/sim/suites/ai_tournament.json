{
  "suite_id": "ai_tournament",
  "description": "Every AI the game knows, the shipped garrison AI, the experimental rush AI from the pack, and random, plays every other on the outpost from both sides. The standings table, not this suite's assertions, is the answer to which one to ship for which faction; the assertions only pin what must hold for the table to mean anything.",
  "runs": 30,
  "seed": 400,
  "register": ["res://ai/raider_pack.gd"],
  "tournament": {
    "ais": ["random", "garrison", "rush"],
    "battles": ["res://battles/outpost.json"],
    "swap_sides": true
  },
  "assertions": [
    {"metric": "matrix.garrison.random", "comparator": "gte", "expected": 0.6},
    {"metric": "matrix.rush.random", "comparator": "gte", "expected": 0.6},
    {"metric": "standings.random.win_rate", "comparator": "lte", "expected": 0.3},
    {"metric": "standings.rush.games", "comparator": "eq", "expected": 120},
    {"metric": "metrics.events.rejected", "comparator": "eq", "expected": 0}
  ]
}
