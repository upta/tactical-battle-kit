{
  "suite_id": "skirmish_tournament",
  "description": "Every skirmish AI against every other on two maps with sides swapped: the standings say who is best and the matrix says by how much against whom. The kit claims greedy beats random 75% of the time on the open field; the tournament asks whether that survives a chokepoint and a side swap. Read the matrix per map below the standings: the chokepoint is where greedy is weakest.",
  "runs": 30,
  "seed": 4000,
  "tournament": {
    "ais": ["random", "greedy"],
    "battles": [
      "res://examples/skirmish/battles/open_field.json",
      "res://examples/skirmish/battles/chokepoint.json"
    ],
    "swap_sides": true
  },
  "assertions": [
    {"metric": "standings.greedy.win_rate", "comparator": "gte", "expected": 0.7},
    {"metric": "standings.greedy.games", "comparator": "eq", "expected": 120},
    {"metric": "matrix.greedy.random", "comparator": "gte", "expected": 0.7},
    {"metric": "win_rate.red", "matchup": "open_field:greedy_vs_random", "comparator": "gte", "expected": 0.75},
    {"metric": "draw_rate", "comparator": "lte", "expected": 0.15}
  ]
}
