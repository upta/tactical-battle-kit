@tool
extends EditorPlugin

# The kit is plain scripts and resources; the plugin exists so the editor can
# enable or disable it as one unit. No editor UI is registered.


func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass
